{
  "name": "agfs-dev",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r typecheck",
    "cli": "pnpm --filter @agfs/cli build && node packages/cli/dist/index.js",
    "dev": "pnpm --filter @agfs/web dev",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
